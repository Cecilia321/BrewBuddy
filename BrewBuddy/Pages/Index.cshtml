@page
@model IndexModel
@{
    ViewData["Title"] = "BrewBuddy - Welcome";
}

    <title>@ViewData["Title"]</title>
    <style>
        /* Login form styling */
        .login-container {
            flex-grow: 1;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        .login-box {
            background-color: #ffffff; /* Hvid baggrund */
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0px 6px 10px rgba(0, 0, 0, 0.15);
            width: 350px;
            text-align: center;
        }

        .login-box label {
            font-weight: bold;
            font-size: 1.1rem;
            display: block;
            color: #3c2f2f; /* Mørk brun */
            margin-bottom: 5px;
            text-align: left;
        }

        .form-control {
            width: 100%;
            padding: 10px;
            margin: 10px 0 20px;
            border: 1px solid #ccc;
            border-radius: 6px;
            transition: border-color 0.3s;
        }

        .form-control:focus {
            border-color: #6f4e37;
            outline: none;
        }

        .btn-primary {
            background-color: #38281e;
            color: white;
            border: none;
            padding: 12px 20px;
            cursor: pointer;
            border-radius: 6px;
            font-size: 1rem;
            width: 100%;
            transition: background-color 0.3s, transform 0.2s;
        }
 body {
     background-image: url('/Images/kaffebaggrund.jpg');
     background-repeat: no-repeat;
     background-size: cover;
 }
    </style>

<section>
    <!-- Header with site name -->
    @* <article class="text-center">
        <h1>BrewBuddy</h1>
    </article> *@

    <!-- Login form in the middle of the page -->
    <div class="login-container">
        <div class="login-box">
            <form method="post">
                <label asp-for="Email">Email</label>
                <input type="text" asp-for="Email" class="form-control" />
                <span asp-validation-for="Email" class="text-danger"></span>

                <label asp-for="Password">Password</label>
                <input type="password" asp-for="Password" class="form-control" />
                <span asp-validation-for="Password" class="text-danger"></span>

                <input type="submit" class="btn-primary" value="Login" />
            </form>
        </div>
    </div>
</section>


